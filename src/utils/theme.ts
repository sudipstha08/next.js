const colors = {
  blue: "#007bff",
  green: "#28a745",
  red: "#dc3545",
  tomato: "Tomato",
  lightblue: "#17a2b8",
  dark: "#343a40",
} as const;

const fonts = {
  ubuntu: "Ubuntu, sans-serif",
} as const;

export { colors, fonts };
